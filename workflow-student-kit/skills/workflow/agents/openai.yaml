interface:
  display_name: "Workflow"
  short_description: "Resume delivery with progress summaries and change checkpoints"
  default_prompt: "Use $workflow to start or resume this project, summarizing its goal, progress, next action, and OpenSpec paths first."
